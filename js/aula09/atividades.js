// 1. Crie um array com o nome de 5 jogos, sem nomes repetidos, e use forEach para exibir valor, índice e array.
// let jogos = ["Forza Motorsport", "God of War", "Far Cry", "Valorant", "Watch Dogs"];
// jogos.forEach((valor, indice, array) => {
//     console.log(`Valor: ${valor} , Índice: ${indice} , Array: [${array}]`);
// });

// 2. Dado o array numbers, utilize o método map para criar um novo array contendo o dobro de cada elemento.
// let numbers = [1, 2, 3, 4, 5];
// let dobroNumbers = numbers.map(numero => numero * 2);
// console.log(dobroNumbers); // Resultado esperado: [2, 4, 6, 8, 10]

// 3. Dado o array numbers, utilize o método filter para criar um novo array contendo apenas os números pares.
// let numerosPares = numbers.filter(numero => numero % 2 === 0);
// console.log(numerosPares); // Resultado esperado: [2, 4]

// // 4. Dado o array fruits, utilize o método forEach para exibir cada fruta no console.
// let fruits = ["apple", "banana", "orange", "kiwi"];
// fruits.forEach(fruta => console.log(fruta));

// // 5. Crie o array idades e utilize filter para selecionar idades >= 18 e forEach para mostrar uma mensagem com a idade.
// let idades = [16, 48, 23, 22, 45, 8, 12];
// let idadesAdultas = idades.filter(idade => idade >= 18);
// idadesAdultas.forEach(idade => console.log(`Sua idade é: ${idade}`));
